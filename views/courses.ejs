<%- include('partials/head') %>
<%- include('partials/navbar') %>

<main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-8">
  <section class="mb-10">
    <h1 class="text-4xl font-bold gold-text">Forex Courses</h1>
    <p class="mt-3 text-gray-300">Structured forex trading course in Calicut and Calicut with beginner to advanced strategy execution, available for learners across Kerala.</p>
  </section>

  <section class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    <% courses.forEach((course, idx) => { %>
      <%- include('partials/course-card', { course, featured: idx === 0 }) %>
    <% }) %>
  </section>
</main>

<%- include('partials/footer') %>

